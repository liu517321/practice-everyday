public class Solution {
    public int Fibonacci(int n) {
        if(n > 0 && n <=2) 
            return 1;
        else if(n > 2) {
            return Fibonacci(n - 1) + Fibonacci(n - 2);
        }else
            return 0;
    }
}
